include button

mixin form(idname='check1')
   form(action='./php/mail.php')
      input(type="text" placeholder="Имя" name="nameClient" autocomplete='given-name' required)
      input(type="tel" class="phone" name="phoneClient" placeholder="+ 7 (___) ___-__-__" required autocomplete='tel')
      input(type="email" placeholder="E-mail" name="emailClient" autocomplete='email' required)
      button(type='submit').button.green Оставить завку
      .checkbox
         input(type="checkbox" name='check' id='' + idname value='yes' required)
         label(for="" + idname)
            span Заполняя форму на сайте, вы соглашаетесь на обработку персональных данных, а также с нашей 
               a(href="#") Политикой конфиденциальности